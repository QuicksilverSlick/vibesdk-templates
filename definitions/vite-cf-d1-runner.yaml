name: "vite-cf-d1-runner"
description: "Vite/React app on Cloudflare Workers with a real D1 (SQLite) database via Drizzle ORM and email/password authentication via better-auth. Preview and production share the same database, so data persists across deploy."
base_reference: "vite-reference"
projectType: app

# Re-enabled with the remote-bindings fix. The preview blocker was: the D1
# template is the only one with a remote:true binding, but the shared vite.config
# called cloudflare() with no options, so @cloudflare/vite-plugin never enabled
# remote bindings -> worker boot failed -> Vite never bound its port. Fixed by
# cloudflare({ remoteBindings: true }) in reference/vite-reference/vite.config.ts
# (per-binding opt-in, no-op for local templates). Zip-write race also fixed
# earlier (dreamforge-cf #225). Flip back to true if a test build still fails.
disabled: false

# Deep-merged into the reference package.json (inherit_dependencies defaults true).
package_patches:
  name: "vite-cf-d1-runner"
  dependencies:
    better-auth: "^1.6.23"
    drizzle-orm: "^0.45.2"
  devDependencies:
    drizzle-kit: "^0.31.10"
    # Pinned exact: remote bindings require the current toolchain, and floating
    # these versions has broken template builds before.
    "@cloudflare/vite-plugin": "1.43.0"
    wrangler: "4.107.0"
  scripts:
    db:generate: "drizzle-kit generate"
    db:migrate:local: "wrangler d1 migrations apply vibesdk-app-db --local"
    db:migrate:remote: "wrangler d1 migrations apply vibesdk-app-db --remote"
